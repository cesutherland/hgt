# Issue 119 — chore: audit agent memories — graduate hardened rules into repo docs

- **Issue:** #119
- **URL:** https://github.com/cesutherland/hgt/issues/119
- **Branch:** cesutherland/119-audit-agent-memories-graduate-hardened
- **State:** in-progress (local)

## Task (verbatim from the issue body)

## Context
Recent sessions cached review-cost rules as agent session memories. Memory is warm context and the unattended path reads none (#17) — repo law living only in memory guarantees the two surfaces drift.

## Scope
Audit the local memory store against: every entry is personal taste, a machine quirk, a pointer to repo law, or transient state with an expiry — nothing else. Delete what CLAUDE.md already records; shrink entries graduated by #116 to pointers.

## Acceptance criteria
- [ ] No rule exists only in memory.
- [ ] Recurring audit noted (or folded into the #116 graduation rule).

## Dependencies / ordering
After #116 lands. Cites #17.


## Getting started

1. Read this file and CLAUDE.md before starting.
2. Implement the slice; commit early and often — every commit is a recovery checkpoint.
3. Open a PR for human review. Do not merge (green is necessary, not sufficient).

## Recovery

If this session dies: re-read this file, check `git status`, then resume the named session
`hgt/119-audit-agent-memories-graduate-hardened` (`claude --resume`). git is the durable work state — not the agent's memory.

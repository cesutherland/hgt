# Issue 111 — chore: scrub the operator's name from repo files

- **Issue:** #111
- **URL:** https://github.com/cesutherland/hgt/issues/111
- **Branch:** cesutherland/111-scrub-operator-s-name-from
- **State:** in-progress (local)

## Task (verbatim from the issue body)

## Problem
~21 tracked-file hits on the operator's given name (ADRs, workflow, `.hgt/work/`). Seats, not names.

## Scope
Replace with role terms (operator / reviewer / the human). Rule going forward: the name never lands in committed text.

## Non-goals
History rewrite; the GH username; issue/PR bodies.

## Acceptance criteria
- [ ] `rg -i` on the git author's given name: zero tracked hits.


## Getting started

1. Read this file and CLAUDE.md before starting.
2. Implement the slice; commit early and often — every commit is a recovery checkpoint.
3. Open a PR for human review. Do not merge (green is necessary, not sufficient).

## Recovery

If this session dies: re-read this file, check `git status`, then resume the named session
`hgt/111-scrub-operator-s-name-from` (`claude --resume`). git is the durable work state — not the agent's memory.

# Issue 51 — feat: GitHub Actions executor — ready issue → PR (live body; snapshot deferred)

- **Issue:** #51
- **URL:** https://github.com/cesutherland/hgt/issues/51
- **Branch:** cesutherland/51-github-actions-executor-ready-issue
- **State:** in-progress (local)

## Task (verbatim from the issue body)

## Context
This is the point of hgt — a `ready` issue that becomes a PR with no local
babysitting — and it has no ticket. The queue jumped to hardening the trust
boundary (#39/#40/#41) and skipped the executor the boundary exists to feed.
Shortest path to iterating in GitHub Actions.

v0 reads the **live issue body**, not a frozen snapshot: safe because the
executor is poor & powerless (§3), and Carl solely curates issues right now, so
the TOCTOU/invisible-unicode surface is self-inflicted. Normalize + snapshot
(#39/#40/#41) are the fast-follow that flips the trigger to read the snapshot.

## Scope
`.github/workflows/hgt-execute.yml`:
- Trigger: `issues: labeled`, gated to `ready`.
- `permissions: contents: write, pull-requests: write` — nothing more.
- Secrets: `ANTHROPIC_API_KEY` only.
- `claude-code-action` pinned to a SHA for **≥ v1.0.94** (auth-bypass fix, §3).
- Build the agent prompt from the live issue body → feature branch → PR.
- Never writes `main`; human is the merge gate.

## Step 0 (do first)
Verify the action's current issue→PR trigger/mode surface (autonomous-on-label
vs `@claude` comment) against its README — §8 says verify before relying on
setup specifics. Shapes the trigger block.

## Acceptance criteria
- [ ] Labeling an issue `ready` opens a PR from a feature branch, no local steps.
- [ ] `ANTHROPIC_API_KEY` repo secret set.
- [ ] Ruleset applied on `main` (require PR + human review; Actions can't approve
      PRs; default read perms) — confirm, apply via `forge_print_ruleset` if not.
- [ ] Workflow holds no secret beyond `ANTHROPIC_API_KEY`; no `id-token: write`.
- [ ] Action pinned to a SHA ≥ v1.0.94.

## Non-goals (deferred)
- Frozen snapshot / normalize / `hgt issue ready` verb — #39/#40/#41.
- Agent-agnostic executor seam — #6.

## Related
Surfaces #35 (phases predate this being the actual entry point). Fast-follow:
#39, #40, #41.


## Checklist

- [ ] Read this file and CLAUDE.md before starting.
- [ ] Implement the slice; commit early and often — every commit is a recovery checkpoint.
- [ ] Open a PR for human review. Do not merge (green is necessary, not sufficient).

## Recovery

If this session dies: re-read this file, check `git status`, then resume the named session
`hgt/51-github-actions-executor-ready-issue` (`claude --resume`). git is the durable work state — not the agent's memory.
